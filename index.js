// Write your code in this file!
const currentUser = 'Grace Hopper';
let welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;
const upperCaseCurrentUser = currentUser.toUpperCase();
let excitedWelcomeMessage =`WELCOME TO FLATBOOK, ${upperCaseCurrentUser}.toUpperCase !`;

const firstInitial = currentUser[0];
const restOfLetters = currentUser.slice(1);
const shortGreeting =  "Welcome, "+''+firstInitial+"!";
